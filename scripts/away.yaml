#-------------------------------------------
# Actions to take when Away detected
#-------------------------------------------
away:
  alias: Away
  sequence:
    - service: lock.lock
      data:
        entity_id: group.all_locks