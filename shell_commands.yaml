#-------------------------------------------
#  Remove doorbell images older than 30 days
#-------------------------------------------
clean_doorbell_images: find /config/www/* -mtime +30 -exec rm {} \;