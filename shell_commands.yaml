#-------------------------------------------
#  Remove doorbell images older than 30 days
#-------------------------------------------
clean_snapshots: find /config/www/snapshots/* -mtime +30 -exec rm {} \;