#-------------------------------------------
# Overhead Garage Door Opened Notice
#-------------------------------------------
- alias: "Chris Garage Door Opened"
  initial_state: true
  hide_entity: true
  trigger:
    - platform: state
      entity_id: cover.chris_garage_door
      from: 'closed'
      to: 'open'
    - platform: state
      entity_id: cover.megan_garage_door
      from: 'closed'
      to: 'open'
  condition:
    condition: state
    entity_id: input_boolean.enable_alerts
    state: 'on'
  action:
    - service: notify.pushover_chris
      data_template:
        title: "\U0001F697 Overhead Garage Door Opened"
        message: "Hi! {{ trigger.to_state.attributes.friendly_name }} has been opened."
        data:
          priority: 1

#-------------------------------------------
# Overhead Garage Door Closed Notice
#-------------------------------------------
- alias: "Chris Garage Door Closed"
  initial_state: true
  hide_entity: true
  trigger:
    - platform: state
      entity_id: cover.chris_garage_door
      from: 'open'
      to: 'closed'
    - platform: state
      entity_id: cover.megan_garage_door
      from: 'open'
      to: 'closed'
  condition:
    condition: state
    entity_id: input_boolean.enable_alerts
    state: 'on'
  action:
    - service: notify.pushover_chris
      data_template:
        title: "\U0001F697 Overhead Garage Door Closed"
        message: "Hi! {{ trigger.to_state.attributes.friendly_name }}has been closed."
        data:
          priority: 1
