#-------------------------------------------
#  Outside Tab
#-------------------------------------------
outside_tab:
  name: Outside
  view: yes
  icon: mdi:tree
  entities:
    - group.garage
    - group.driveway_camera
    - group.front_door_camera
    - group.garage_camera
    - group.front_yard_camera
    - group.deck_camera
    
driveway_camera:
  entities:
    - camera.driveway_camera

front_door_camera:
  entities:
    - camera.front_door_camera

garage_camera:
  entities:
    - camera.garage_camera

front_yard_camera:
  entities:
    - camera.front_yard_camera

deck_camera:
  entities:
    - camera.deck_camera