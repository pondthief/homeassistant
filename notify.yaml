#-------------------------------------------
#  Email Notices
#-------------------------------------------
- name: email
  platform: smtp
  sender: !secret smtp_email
  recipient:
    - !secret chris_email
    - !secret megan_email
  server: !secret smtp_host
  username: !secret smtp_user
  password: !secret smtp_pass
  sender_name: Home Assistant

#-------------------------------------------
#  SMS Notices
#-------------------------------------------
- name: sms
  platform: twilio_sms
  from_number: !secret twilio_num

#-------------------------------------------
#  Pushover Notices
#-------------------------------------------
- name: pushover_chris
  platform: pushover
  api_key: !secret pushover_api
  user_key: !secret pushover_chris